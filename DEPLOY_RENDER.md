# 🚀 ДЕПЛОЙ НА RENDER.COM

## Подготовка

### 1. Убедись что код готов
Все изменения сохранены, `bot.py` исправлен.

### 2. Загрузи код на GitHub (если ещё не загружен)

```bash
cd "C:\Users\Allol\Videos\DP\local coord\DP_bot"

# Инициализируй Git (если не сделано)
git init
git add .
git commit -m "Fixed asyncio event loop issue for Render deploy"

# Создай репозиторий на GitHub и загрузи
git remote add origin https://github.com/ТвойUsername/dp-telegram-bot.git
git branch -M main
git push -u origin main
```

## Деплой на Render

### 1. Создай аккаунт на Render.com
- Зайди на https://render.com
- Sign Up (можно через GitHub)

### 2. Создай новый Web Service
1. Dashboard → **New +** → **Background Worker**
2. **Connect Repository** → выбери свой репозиторий `dp-telegram-bot`
3. Настройки:
   - **Name**: `dp-telegram-bot`
   - **Region**: выбери ближайший (EU для лучшей скорости)
   - **Branch**: `main`
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python bot.py`

### 3. Добавь Environment Variables

В разделе **Environment**:

```
BOT_TOKEN = 8497776722:AAEUeizrcfNiuqpgfGKIApH5nzVXYUkPH-4
CHAT_ID = -1002345678901
FIREBASE_DATABASE_URL = https://dpgames-66d73-default-rtdb.europe-west1.firebasedatabase.app
FIREBASE_KEY_JSON = {"type":"service_account","project_id":"dpgames-66d73",...}
```

**ВАЖНО для FIREBASE_KEY_JSON:**
1. Открой `serviceAccountKey.json`
2. Скопируй **ВСЁ** содержимое (весь JSON целиком)
3. Вставь как значение переменной `FIREBASE_KEY_JSON`
4. Render автоматически обработает JSON

### 4. Запусти деплой
1. Нажми **Create Background Worker**
2. Render начнёт деплой (займёт 2-3 минуты)

### 5. Проверь логи
1. После деплоя, открой **Logs**
2. Должно появиться:
```
🔧 Используем Firebase ключ из переменной окружения
✅ Firebase подключен
🚀 Запуск DepressivePasties Bot...
✅ Бот запущен! Нажми Ctrl+C для остановки.
✅ Firebase слушатель запущен
```

## ✅ ГОТОВО!

Бот работает 24/7 на Render!

## 🔄 Обновление бота

```bash
# Внеси изменения в код
git add .
git commit -m "Update bot"
git push

# Render автоматически задеплоит новую версию!
```

## 💰 Стоимость

- **Free Plan**: 750 часов/месяц бесплатно
- Твой бот использует ~1 час = 1 час работы
- **= БЕСПЛАТНО для одного бота!** 🎉

## 🆘 Решение проблем

### Ошибка: "RuntimeError: no running event loop"
✅ **ИСПРАВЛЕНО!** Убедись что используешь исправленную версию `bot.py`

### Бот не запускается
1. Проверь переменные в Render: **Environment** → проверь все 4 переменные
2. Проверь логи: **Logs** → ищи ошибки

### Firebase ошибка
1. Убедись что `FIREBASE_KEY_JSON` содержит **полный** JSON
2. JSON должен начинаться с `{"type":"service_account"...`
3. Не должно быть лишних пробелов или переносов

### Сообщения не синхронизируются
1. Проверь `CHAT_ID` - должен быть ID группы (начинается с `-`)
2. Убедись что бот добавлен в группу
3. Проверь что бот имеет права админа в группе (для отправки сообщений)

### Как получить CHAT_ID:
1. Создай Telegram группу
2. Добавь своего бота в группу
3. Добавь [@getidsbot](https://t.me/getidsbot) в группу
4. Он напишет Chat ID (например: `-1002345678901`)
5. Добавь этот ID в Render Environment Variables

## 📊 Мониторинг

- **Логи**: Render Dashboard → Logs (в реальном времени)
- **Метрики**: Render Dashboard → Metrics (CPU, память)
- **Рестарт**: Если нужно перезапустить → Manual Deploy → Deploy latest commit

## 🔗 Полезные ссылки

- [Render Docs](https://render.com/docs)
- [Python on Render](https://render.com/docs/deploy-python)
- [Telegram Bot API](https://core.telegram.org/bots/api)

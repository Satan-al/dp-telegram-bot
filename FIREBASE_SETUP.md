# Как добавить FIREBASE_KEY_JSON в Render

## Шаг 1: Открой скачанный файл serviceAccountKey.json

Файл выглядит примерно так:
```json
{
  "type": "service_account",
  "project_id": "dpgames-66d73",
  "private_key_id": "abc123...",
  "private_key": "-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n",
  "client_email": "firebase-adminsdk-xxxxx@dpgames-66d73.iam.gserviceaccount.com",
  "client_id": "123456789",
  "auth_uri": "https://accounts.google.com/o/oauth2/auth",
  "token_uri": "https://oauth2.googleapis.com/token",
  ...
}
```

## Шаг 2: Скопируй ВЕСЬ этот JSON

ВАЖНО: Скопируй от первой `{` до последней `}` включительно!

## Шаг 3: В Render.com → Environment Variables

Создай переменную:
```
KEY: FIREBASE_KEY_JSON
VALUE: [вставь скопированный JSON ЦЕЛИКОМ]
```

Должно получиться что-то вроде:
```
FIREBASE_KEY_JSON={"type":"service_account","project_id":"dpgames-66d73",...}
```

ВСЁ В ОДНУ СТРОКУ или можно с переносами - Render поймёт!

## ❗ ВАЖНО:

- НЕ загружай serviceAccountKey.json в GitHub!
- Он уже в .gitignore, но убедись что его там нет
- Используй его ТОЛЬКО как переменную окружения

---

## Проверка:

После добавления всех переменных в Render, бот должен запуститься и в логах будет:
```
🔧 Используем Firebase ключ из переменной окружения
✅ Firebase подключен
```
